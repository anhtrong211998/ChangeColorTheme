<Window x:Class="ChangeColorTheme.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChangeColorTheme"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="{DynamicResource MaterialDesignBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ToggleButton Grid.Row="0" Content="Dark-Light" x:Name="dark_light" Checked="ToggleButton_Checked" Unchecked="dark_light_Unchecked" />
        <Button Grid.Row="1" x:Name="Blue" Content="Blue" Click="Blue_Click" Background="{DynamicResource MaterialDesignPaper}" />
        <Button Grid.Row="2" x:Name="Yellow" Content="Yellow" Click="Yellow_Click" Background="{DynamicResource MaterialDesignPaper}" />
    </Grid>
</Window>
